#include<stdio.h>

int main() {
    int n;
    printf("Insert a number: ");
    scanf("%d", &n);
    if(n == 1) {
    printf("My ball's are heavy :P");
    } else {
        printf("I got ball's in my jaw");
    }
    return 0; 
}